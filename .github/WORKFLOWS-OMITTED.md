# Workflow files omitted

Upstream GitHub Actions workflow files are excluded from this portable source branch because a repository-scoped Actions token is not permitted to publish workflow definitions. Application source, tests, package configuration, scripts, documentation, and the complete binary patch are unaffected.
